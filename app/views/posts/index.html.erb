<div class="row">
  <div class="col-md-8">
    <h1> <%= t ('PostIn.h1')%></h1>
    <p class="lead"> <%= t ('PostIn.h2')%></p>
    <%= render partial: 'posts/post', collection: @posts, locals: { show_topic: true } %>
    <%= will_paginate @posts %>
  </div>
  <div class="col-md-4">
  </div>
</div>
